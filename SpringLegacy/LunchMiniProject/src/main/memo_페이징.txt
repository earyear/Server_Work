페이징 시 필요한 것:)
1. page 번호 | 2. size 페이지당 보여줄 리스트 개수 | 3. getskip() 몇개씩 건너뛰는지

서버 > 화면

전체 데이터 개수 : 75 ~~~> 끝 8페이지
전체 데이터 개수 : 100 ~~~> 끝 10페이지
[조건]
 전체 개수 / 10(size크기) > 올림.
현재 총 데이터 개수 : 1792 ~~~> 끝 180페이지

계산하다보면 끝페이지 기본 : 10페이지...



[조건]
** 전체 끝나는 페이지 개수 > 기본 10(end) ? end : 전체 끝나는 페이지 개수
last(8) > end(10) ? end : last
last(15) > end(10) ? end : last

this. end = end(10) > last(8) ? last(8) : end(10);
this. end = end(10) > last(15) ? last(15) : end(10);